[package]
name = "cashu-gateway-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
cdk-common = { git = "https://github.com/cashubtc/cdk.git", tag = "v0.16.0-rc.2" }
